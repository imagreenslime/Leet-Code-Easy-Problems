# https://leetcode.com/problems/intersection-of-two-arrays-ii/
def intersect(nums1, nums2):
    hash = []
    for a in range(len(nums1)):
        if nums1[a] in nums2:
            hash.append(nums1[a])
            nums2.remove(nums1[a])
            # remove so repeats arent saved
    return hash
